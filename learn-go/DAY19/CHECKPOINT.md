# DAY19: Checkpoint

**Closed book.**

1. What does `BEGIN TRANSACTION` / `COMMIT` / `ROLLBACK` do?

2. Why is `defer tx.Rollback()` safe even after `tx.Commit()`?

3. In the BulkSave drill, what happens to `ddd` and `eee` when `aaa` (duplicate) fails?

4. What's the difference between `db.Exec` and `tx.Exec`?

5. When would you NOT use a transaction?
