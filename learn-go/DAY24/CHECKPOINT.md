# DAY24: Checkpoint

**Closed book.**

1. How do you start a goroutine?

2. What happens if two goroutines write to a map at the same time without a mutex?

3. What's the difference between `sync.Mutex` and `sync.RWMutex`?

4. When should you use `RLock` vs `Lock`?

5. What does `defer r.mu.Unlock()` do and why is it important?
