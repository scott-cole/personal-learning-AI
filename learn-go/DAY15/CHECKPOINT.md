# DAY15: Checkpoint

**Closed book.**

1. Write a SELECT query to get the `original_url` for a given `code`.

2. What does `INSERT INTO urls (code, original_url) VALUES ('x', 'y')` do?

3. What's the difference between `WHERE access_count = 0` and `WHERE access_count IS NULL`?

4. Why is `SELECT *` discouraged in production code?

5. What does `PRIMARY KEY AUTOINCREMENT` do?
