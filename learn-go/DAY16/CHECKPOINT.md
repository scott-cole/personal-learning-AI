# DAY16: Checkpoint

**Closed book.**

1. What does `_ "github.com/mattn/go-sqlite3"` do? Why the underscore?

2. What's the difference between `db.Exec`, `db.QueryRow`, and `db.Query`?

3. Why do we use `?` placeholders instead of string concatenation?

4. What does `.Scan(&var1, &var2)` do?

5. What happens if you forget `rows.Close()`?
