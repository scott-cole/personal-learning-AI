# DAY26: Checkpoint

**Closed book.**

1. How does a token bucket rate limiter work?

2. What HTTP status code should a rate limiter return?

3. What's the `Retry-After` header for?

4. Why is rate limiting implemented as middleware rather than inside each handler?

5. How would you implement per-IP rate limiting?
