# DAY22: Checkpoint

**Closed book.**

1. Why should you avoid hardcoded values like `const port = 8080`?

2. What does `os.Getenv("PORT")` return if `PORT` is not set?

3. What's the difference between `os.Getenv` and `flag.String`?

4. Why is it important to validate config at startup rather than when the first request arrives?

5. Write a function `getEnvBool(key string, defaultVal bool) bool`.
