# DAY27: Checkpoint

**Closed book.**

1. What's the purpose of the `cmd/` directory?

2. What does `internal/` do that's special in Go?

3. Why can't `internal/handler` import `internal/storage` directly, but `internal/service` can?

4. What problem does the standard project layout solve?

5. When should you NOT use this layout?
