# DAY17: Checkpoint

**Closed book.**

1. What problem do migrations solve?

2. What's the `schema_migrations` table for?

3. Why must migrations be applied in order?

4. What happens if you change a migration file after it's been applied?

5. Why does the `code` column use `TEXT PRIMARY KEY` instead of `INTEGER AUTOINCREMENT`?
